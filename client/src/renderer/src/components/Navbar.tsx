import { Box } from '@chakra-ui/react'

export const Navbar = (): JSX.Element => {
  return (
    <Box
      as="header"
      display="flex"
      justifyContent="space-between"
      alignItems="center"
      h="6rem"
      bg="red"
    >
      header
    </Box>
  )
}
